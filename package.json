{
  "name": "tb-apps-sdk",
  "private": true,
  "scripts": {
    "clean": "lerna clean --yes && rm -rf node_modules",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "prerelease": "yarn lint && yarn test && yarn build && lerna run prepublish",
    "precommit": "lerna run precommit",
    "release": "lerna publish --contents ./lib"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
